(function(PLUGIN_ID) {
  'use strict';

  var config = kintone.plugin.app.getConfig(PLUGIN_ID);
  
  // get field list

  // populate source field dropdown with appropriate options from field list

  // based on selection of source field dropdown, generate target field list
  
  // populate target field dropdown with options from new target field list

  // click button to initiate migration

})(kintone.$PLUGIN_ID);
